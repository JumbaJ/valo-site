# Copy to .env.local for local dev. Real values go in Vercel env vars — never commit them.
# Phase 2 — flip to 1 once the data proxy is live:
VITE_LIVE_DATA=0
# Your Ubuntu data-proxy base URL (Phase 2):
VITE_API_BASE=https://api.valotrading.app
